# Secrets
.env
*.key
*.pem

# Logs
logs/
*.log

# Reports (regenerated daily, don't commit)
reports/
data/

# OS
.DS_Store
__pycache__/
*.pyc

# Python
*.egg-info/
.venv/
venv/

# Tooling state (don't commit)
.mavis/
.wrangler/
node_modules/

# === INCLUDED in repo (override above) ===
# public/      — static dashboard for Cloudflare Pages (must commit)
# hk_universe_200.json — top 200 HK tickers (must commit)
# us_universe_200.json — US tickers (must commit)
